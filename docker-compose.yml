services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: loteria-api
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: "Development"
      ASPNETCORE_URLS: "http://+:8080"
      ConnectionStrings__DefaultConnection: "Server=mysql;Port=3306;Database=${MYSQL_DATABASE};User=root;Password=${MYSQL_ROOT_PASSWORD};"
      Jwt__Key: "${JWT_KEY}"
      Jwt__Issuer: "${JWT_ISSUER:-LoteriaVirtualAPI}"
      Jwt__Audience: "${JWT_AUDIENCE:-UsuariosDeLoteria}"
    depends_on:
      mysql:
        condition: service_healthy

  mysql:
    image: mysql:8.0
    container_name: loteria-mysql
    command: --lower_case_table_names=1
    environment:
      MYSQL_ROOT_PASSWORD: "${MYSQL_ROOT_PASSWORD}"
      MYSQL_DATABASE: "${MYSQL_DATABASE:-loteriavirtual}"
    ports:
      - "3307:3306"
    volumes:
      - mysql_data_lc:/var/lib/mysql
      - ./Scripts/Esquema_Inicial_MODIFICADO.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -uroot -p$${MYSQL_ROOT_PASSWORD} --silent"]
      interval: 5s
      timeout: 5s
      retries: 20

volumes:
  mysql_data_lc:
